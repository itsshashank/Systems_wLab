DECLARE
   STR VARCHAR(20);
   LEN NUMBER;
   STR1 VARCHAR(20);
   I NUMBER; 
BEGIN
    STR:='&STR';
	LEN:=LENGTH(STR);
	FOR I IN REVERSE 1..LEN LOOP
		STR1:=STR1||SUBSTR(STR,I,1);
	END LOOP;
	DBMS_OUTPUT.PUT_LINE(STR1);
END;
/