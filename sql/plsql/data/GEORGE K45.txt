CREATE TABLE AUDIT(ID INTEGER,
						OLD_SALARY DECIMAL(7,2),NEW_SALARY DECIMAL(7,2),UPDATION_DATE DATE);
CREATE  TRIGGER SAL_CHANGES AFTER
UPDATE OF BASICPAY ON EMP2
FOR EACH ROW
BEGIN
INSERT INTO AUDIT VALUES
(:OLD.ID,:OLD.BASICPAY,:NEW.BASICPAY,SYSDATE);
END;
/
UPDATE 
